#include<stdio.h>
int main(){
int list[5];
int first,last;
int sum,num;
printf("enter the number ");
for(i=0;i<5;i++){
scanf("%d",&num);
list[i]=num;
}
first=[0];
last=[4];
sum=first+last;
printf("sum of first and last term is %d",sum);
}
  return 0;
 
